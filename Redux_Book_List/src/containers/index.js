/*
  A container component (aka smart component) is a component with a direct relationship
  with the redux application state.
*/

import BookList from './book-list';
import BookDetail from './book-detail';

export {
  BookList,
  BookDetail
}
